# Implementation Plan
> Generated by Claude (Architect) — 2026-07-29
> Goal: hot-path sweep — จัดสรรทุกเฟรม · material leak · `Camera.main` ไม่ cache
> Round: 5+6 รวมเป็นรอบเดียว · **14 tasks · 14 ไฟล์**

## Context

**นี่คือของใหม่ ไม่ใช่ backlog เดิม** — audit รอบ 1-3 อ่าน authority/logic/config
แต่**ไม่เคยมีรอบไหนกวาด hot path** ทั้งที่เกมนี้เป็น bullet-heaven ที่มีศัตรูหลักร้อยตัวพร้อมกัน

backlog เดิมจาก audit **ถูกเก็บไปเกือบหมดใน Round 1-4** ที่เหลือคือของที่ต้องแก้
`.asset`/`.prefab` (กฎห้าม) หรือของที่ยังไม่ชัดพอจะสั่งงาน — ดูท้ายไฟล์

### รูปแบบร่วมอีกครั้ง — "แก้ไฟล์เดียว ลืมพี่น้อง" ครั้งที่ 6

| แก้แล้ว | ยังไม่แก้ (= งานรอบนี้) |
|---|---|
| `TelegraphZone` ใช้ `sharedMaterial` + `MaterialPropertyBlock` | `FloorHazard` · `BossTether` · `Enemy` ยังใช้ `.material` |
| `WorldHPBar:93,102` · `ObjectiveIndicatorUI:103,177` cache `Camera.main` | `BillboardFaceCamera` · `FloatingBuffUI` **ไม่ cache** |
| `Enemy.cs:261` ใช้ `OverlapSphereNonAlloc` | `MineObject:48,58` ใช้ `OverlapSphere` ในลูปทุกเฟรม |

**`.material` เป็นอันตรายเพราะการอ่านครั้งแรก clone material ใหม่ทั้งชิ้นต่อ renderer**
และ Unity ไม่คืนให้ตอน object ตาย — audit เคยจับได้ที่ `TelegraphZone` แล้ว แต่ลงแค่ไฟล์เดียว

## Project Rules (ห้ามละเมิด)

- server-authoritative · VFX ผ่าน `NetworkedVFXPool` · SFX ผ่าน `SoundManager` ·
  `Time.timeScale` ผ่าน `GamePause` เท่านั้น
- ห้ามเพิ่มพารามิเตอร์ combat เข้า ServerRpc ตัวใหม่
- ห้ามแตะ `Library/` `Temp/` `obj/` `Build/` · ห้ามแตะ `.prefab` / `.unity` / `.asset`
- **ห้ามเปลี่ยน access modifier / signature ของสมาชิกที่มีอยู่** — เพิ่มของใหม่ได้ แก้ของเดิมไม่ได้
- **ห้ามเพิ่มโค้ด "กันไว้ก่อน" ที่แผนไม่ได้สั่ง** — ไม่มี `else` สำรอง ไม่มี null-check เกิน
  ไม่มี fallback ที่ไม่ได้ขอ *(Round 3 และ Round 4 ทำแบบนี้อย่างละครั้ง ต้องย้อนกลับทั้งคู่)*
- **ห้ามเปลี่ยนสิ่งที่ผู้เล่นเห็น** — สี ขนาด ความเร็ว จังหวะ ต้องเหมือนเดิมเป๊ะ
  รอบนี้เปลี่ยน**วิธีทำ** ไม่ใช่**ผลลัพธ์**

## Tasks

- [ ] T1: `playermove` เลิก `GetComponent` ทุกเฟรม
  - File: `Assets/Script/playermove.cs` (MODIFY)
  - `:118` `GetComponent<PlayerStatManager>()?.GetDurationMultiplier()` และ
    `:154` `IsOwner ? GetComponent<PlayerStatManager>() : null` — **เรียกทุกเฟรมทั้งคู่**
  - เพิ่ม field cache แล้วเซ็ตครั้งเดียวใน `Awake()` หรือ `OnNetworkSpawn()`:
    ```csharp
    PlayerStatManager _statManager;
    ```
    แล้วให้ทั้งสองจุดใช้ `_statManager` แทน
  - **ห้ามเปลี่ยนเงื่อนไข `IsOwner` ที่ `:154`** — เก็บ logic เดิมไว้ เปลี่ยนแค่ที่มาของ reference
  - Expected: ไม่มี `GetComponent<PlayerStatManager>()` เหลือใน `Update()` หรือเมธอดที่ถูกเรียกทุกเฟรม

- [ ] T2: `FloorHazard` เลิก `GetComponent` + `.material` ทุกเฟรม
  - File: `Assets/Script/FloorHazard.cs` (MODIFY)
  - `:137-139` ในลูปทุกเฟรม:
    ```csharp
    var dangerRend = dangerVisual.GetComponent<Renderer>();
    if (dangerRend != null)
        dangerRend.material.color = dangerCol;
    ```
  - cache `Renderer` ไว้ตอน setup แล้วเปลี่ยนสีผ่าน **`MaterialPropertyBlock`**
    **ลอกรูปแบบจาก `TelegraphZone.cs`** (`:52` `private MaterialPropertyBlock mpb;` ·
    `:244` `mpb ??= new MaterialPropertyBlock();` · `:684-687` การใช้กับ `sharedMaterial`)
  - Expected: ไม่มี `GetComponent` และไม่มี `.material` ในโค้ดที่รันทุกเฟรมของไฟล์นี้
  - Why: `.material` clone material ใหม่ต่อ renderer และ `FloorHazard` ถูก spawn ซ้ำทุกครั้งที่บอสออกท่า

- [ ] T3: `FloorHazard` ตอน setup ใช้ `sharedMaterial`
  - File: `Assets/Script/FloorHazard.cs` (MODIFY) — ไฟล์เดียวกับ T2 แต่คนละจุด
  - `:160` `dangerVisual.GetComponent<Renderer>().material = mat;`
  - `:180` `safe.GetComponent<Renderer>().material = safeMat;`
  - เปลี่ยนเป็น `sharedMaterial` ทั้งสองจุด
  - **ถ้าเปลี่ยนแล้วพบว่ามีการแก้สีของ material ตัวนั้นทีหลัง (ซึ่งจะไปโดน asset ต้นฉบับ)
    — หยุด เขียนใต้ `## Questions`** อย่าเดา

- [ ] T4: `BossTether` เลิก clone material
  - File: `Assets/Script/BossTether.cs` (MODIFY)
  - `:246` `lineRenderer.material = mat;` · `:268` `rend.material = mat;` → `sharedMaterial`
  - `:272` `rend.material.color = pillarColor;` → ใช้ `MaterialPropertyBlock` แบบ T2
  - Expected: ไม่มี `.material` เหลือในไฟล์นี้
  - Why: tether ถูก spawn ทุกครั้งที่บอสออกท่า Tether — leak สะสมตลอดการต่อสู้

- [ ] T5: `Enemy` เลิก clone material ตอน freeze
  - File: `Assets/Script/Enemy.cs` (MODIFY)
  - `NotifyFreezeClientRpc` (`:495-510`) เรียก `GetComponentInChildren<MeshRenderer>()`
    แล้ว `mr.material.color = ...` **ทุกครั้งที่ freeze/unfreeze**
  - cache `MeshRenderer` ไว้ (เช่นใน `OnNetworkSpawn`) แล้วเปลี่ยนสีผ่าน `MaterialPropertyBlock`
  - **ห้ามเปลี่ยนสี** — ice-blue `(0.5f, 0.8f, 1f, 1f)` และ `Color.white` ต้องเหมือนเดิม
  - Expected: ไม่มี `.material` และไม่มี `GetComponentInChildren` ใน `NotifyFreezeClientRpc`
  - Why: `Enemy` เป็น object ที่มีจำนวนมากที่สุดในเกม — หลักร้อยตัว × 1 material clone ต่อตัว

- [ ] T6: `BillboardFaceCamera` cache กล้อง
  - File: `Assets/Script/VFX/BillboardFaceCamera.cs` (MODIFY)
  - `LateUpdate()` เรียก `Camera.main` **ทุกเฟรม** — เพิ่ม field cache
    แล้วใช้รูปเดียวกับ `WorldHPBar:102`: `if (_cam == null) _cam = Camera.main;`
  - Expected: `Camera.main` ถูกเรียกเฉพาะตอน `_cam` เป็น null
  - Why: `Camera.main` ค้นหาด้วย tag · script นี้ติดอยู่บน world-space canvas ทุกตัวในฉาก

- [ ] T7: `FloatingBuffUI` cache กล้อง
  - File: `Assets/Script/UI/FloatingBuffUI.cs` (MODIFY)
  - `:92` และ `:94` เรียก `Camera.main` **สองครั้งต่อเฟรม** — cache แบบเดียวกับ T6
  - **ห้ามเปลี่ยน `transform.rotation = cam.transform.rotation;`** — เก็บพฤติกรรม billboard เดิม

- [ ] T8: `MineObject` เลิกจัดสรร array ทุกเฟรม
  - File: `Assets/Script/MineObject.cs` (MODIFY)
  - `:48` `var hits = Physics.OverlapSphere(...)` **ในลูปทุกเฟรม** — คืน array ใหม่ทุกครั้ง
  - `:58` `foreach (var c in Physics.OverlapSphere(...))` เช่นกัน
  - เปลี่ยนเป็น `Physics.OverlapSphereNonAlloc` พร้อม buffer ที่ประกาศเป็น field
    **ลอกรูปแบบจาก `Enemy.cs:261`** ซึ่งใช้ `OverlapSphereNonAlloc` อยู่แล้ว
  - **ระวัง**: `NonAlloc` คืน**จำนวนที่เจอ** และ buffer อาจมีของเก่าค้างเกินจำนวนนั้น
    ต้องวนแค่ `0..count-1` **ห้ามวนทั้ง buffer**
  - Expected: ไม่มี `Physics.OverlapSphere(` เหลือในไฟล์นี้

- [ ] T9: `DevTools` canvas ซ้อนทุกครั้งที่โหลดฉากใหม่
  - File: `Assets/Script/DevTools.cs` (MODIFY)
  - `:274` `DontDestroyOnLoad(canvasGO);` แต่ตัว `DevTools` เป็น scene object
    → โหลดฉากใหม่ = `DevTools` ตัวใหม่สร้าง canvas ใหม่ ส่วนตัวเก่าค้างอยู่ **สะสมไปเรื่อยๆ**
  - แก้ด้วยการเช็คว่ามี canvas เดิมอยู่แล้วหรือยังก่อนสร้าง (เช่น static reference ไปยัง canvas
    ที่สร้างไว้ แล้ว reuse) **หรือ**ไม่ต้อง `DontDestroyOnLoad` เลยเพราะ script เกิดใหม่ทุกฉากอยู่แล้ว
  - **เลือกทางที่แก้น้อยที่สุด** และเขียนเหตุผลไว้ใน `## Changed Files`
  - Expected: โหลดฉากซ้ำแล้วมี DevTools canvas เพียงตัวเดียว

- [ ] T10: `OrbVisual` เลิก clone material  ← **ปริมาณสูงสุดในเกม**
  - File: `Assets/Script/VFX/OrbVisual.cs` (MODIFY)
  - `:47` `rend.material.color = orbColor;` · `:50` `rend.material.EnableKeyword("_EMISSION");` ·
    `:51` `rend.material.SetColor("_EmissionColor", orbColor * 0.6f);` — **แตะ `.material` 3 ครั้ง**
  - ใช้ `sharedMaterial` + `MaterialPropertyBlock` แบบเดียวกับ T2
  - **`EnableKeyword` ทำผ่าน MPB ไม่ได้** — ถ้าติดตรงนี้ให้คงบรรทัดนั้นไว้แล้วแปลงเฉพาะ
    `color` กับ `SetColor` **และเขียนไว้ใต้ `## Questions`** ว่าเหลือ keyword จุดเดียว
  - **ห้ามเปลี่ยนสีหรือความสว่าง** — `orbColor * 0.6f` ต้องเท่าเดิม
  - Why: orb ถูก spawn **ทุกครั้งที่ศัตรูตาย** — เป็น object ที่เกิดถี่ที่สุดในเกม
    หนึ่ง clone ต่อ orb คือ leak ที่โตเร็วที่สุดในบรรดาทั้งหมดในแผนนี้

- [ ] T11: `HealingOrb` + `MagnetOrb` เลิก clone material
  - File: `Assets/Script/HealingOrb.cs` (MODIFY) — `:40` `rend.material.color = Color.green;`
  - File: `Assets/Script/MagnetOrb.cs` (MODIFY) — `:35` `rend.material.color = Color.magenta;`
  - ใช้ MPB แบบเดียวกับ T10 · **ห้ามเปลี่ยนสี** (เขียว / ม่วงแดง ต้องเท่าเดิม)
  - **ห้ามแตะส่วน `isCollected` guard** ที่ Round 1 ใส่ไว้

- [ ] T12: `WeaponBase` cache กล้องในเส้นทางเล็ง
  - File: `Assets/Script/Weapon/WeaponBase.cs` (MODIFY)
  - `:350` `if (aimMode == AimMode.MouseAim && Camera.main != null)` และ
    `:357` `var ray = Camera.main.ScreenPointToRay(...)` — อยู่ในเส้นทางที่เรียก**ทุกนัดที่ยิง**
  - cache แบบเดียวกับ T6 (`if (_cam == null) _cam = Camera.main;`)
  - **ห้ามเปลี่ยน logic ของ `AimMode`** — เปลี่ยนแค่ที่มาของ reference กล้อง
  - Why: bullet-heaven ยิงหลายนัดต่อวินาที × ผู้เล่นทุกคน

- [ ] T13: `ClusterBombWeapon` + `GunnerGiantRocket` cache กล้อง
  - File: `Assets/Script/Weapon/ClusterBombWeapon.cs` (MODIFY) — `:154` `:156`
  - File: `Assets/Script/Weapon/hero/GunnerGiantRocket.cs` (MODIFY) — `:90` `:92`
  - ทั้งคู่เรียก `Camera.main` **สองครั้งต่อการยิงหนึ่งนัด** — cache แบบเดียวกับ T12
  - **`ClusterBombWeapon` ห้ามแตะ distance gate ที่ `OnEnemyKilled`** — Round 1 ใส่ไว้
  - **`GunnerGiantRocket` ห้ามแตะ `RollDamage(..., out bool _)` ที่ `:68`** — จงใจเว้นไว้ให้ Stage 1

- [ ] T14: ตรวจปิดงาน — **ไม่แก้ไฟล์**
  - grep `\.material\b` (ไม่นับ `sharedMaterial`) — ต้องไม่เหลือใน `FloorHazard.cs` ·
    `BossTether.cs` · `Enemy.cs` · `VFX/OrbVisual.cs` · `HealingOrb.cs` · `MagnetOrb.cs`
  - grep `Camera\.main` — ใน `BillboardFaceCamera.cs` · `FloatingBuffUI.cs` · `WeaponBase.cs` ·
    `ClusterBombWeapon.cs` · `GunnerGiantRocket.cs` ต้องอยู่หลังเงื่อนไข null เท่านั้น
  - grep `Physics.OverlapSphere(` ใน `MineObject.cs` — ต้องไม่เหลือ
  - grep `Time.timeScale` — ยังต้องเจอเฉพาะใน `GamePause.cs`
  - **grep `runner` ในไฟล์ที่แก้ — ต้องอยู่ใน scope จริง** (ดูบทเรียน Round 4 ด้านล่าง)
  - เจออะไรไม่ตรงเขียนใต้ `## Questions` **ห้ามแก้เอง**

## ⚠️ บทเรียนจาก Round 4 — อ่านก่อนเริ่ม

Round 4 **คอมไพล์ไม่ผ่าน** และเป็นความผิดของแผน ไม่ใช่ของ coder

แผนสั่งให้เติม `(runner as BossController)?.RegisterMechanic(no);` ที่ 4 จุด
แต่ **2 จุดอยู่ใน private helper ที่ไม่มี `runner` ใน scope** (`KeepMovingAction.SpawnPunishment` ·
`ColorMatchAoEAction.SpawnColoredZone`) — Claude ตรวจแค่ว่า signature ของคลาสฐานมี `runner`
แล้วสรุปว่าทุก call site เข้าถึงได้ **ซึ่งไม่จริง**

→ **รอบนี้ ก่อนเติมตัวแปรใดๆ ให้เช็คว่ามันอยู่ใน scope ของเมธอดนั้นจริง**
ถ้าไม่อยู่ **อย่าเดา อย่าเปลี่ยน signature เอง** เขียนใต้ `## Questions` แล้วข้ามไป task ถัดไป

## Acceptance Criteria

- คอมไพล์ผ่าน 0 error
- `playermove` ไม่มี `GetComponent<PlayerStatManager>()` ในเส้นทางที่รันทุกเฟรม
- `FloorHazard` · `BossTether` · `Enemy` · `OrbVisual` · `HealingOrb` · `MagnetOrb` ไม่มี `.material` เหลือ
- `BillboardFaceCamera` · `FloatingBuffUI` · `WeaponBase` · `ClusterBombWeapon` ·
  `GunnerGiantRocket` cache `Camera.main`
- `MineObject` ใช้ `OverlapSphereNonAlloc` พร้อม buffer เป็น field
- DevTools canvas ไม่ซ้อนเมื่อโหลดฉากซ้ำ
- **ไม่มีสี ขนาด ความเร็ว หรือจังหวะไหนเปลี่ยน**

## Do NOT

- แก้ได้แค่ 14 ไฟล์: `playermove.cs` · `FloorHazard.cs` · `BossTether.cs` · `Enemy.cs` ·
  `VFX/BillboardFaceCamera.cs` · `UI/FloatingBuffUI.cs` · `MineObject.cs` · `DevTools.cs` ·
  `VFX/OrbVisual.cs` · `HealingOrb.cs` · `MagnetOrb.cs` · `Weapon/WeaponBase.cs` ·
  `Weapon/ClusterBombWeapon.cs` · `Weapon/hero/GunnerGiantRocket.cs`
- **ห้ามแตะ `TelegraphZone.cs` · `WorldHPBar.cs` · `ObjectiveIndicatorUI.cs`** — เป็นต้นแบบทั้งสาม
  ใช้ลอกเท่านั้น
- **ห้ามแตะ `.material` ในไฟล์ที่เหลือ** (`FenceWeapon` `BigAoEWeapon` `OrbitalStrikeWeapon`
  `SupportArenaWeapon` `NetworkedVFXPool`) — ยังไม่ได้ตรวจว่าอยู่ในเส้นทางร้อนหรือไม่ **จงใจเว้น**
- **ห้ามแตะของที่รอบก่อนใส่ไว้** — `isCollected` guard ใน orb (Round 1) · distance gate ใน
  `ClusterBombWeapon.OnEnemyKilled` (Round 1) · `RollDamage(..., out bool _)` ใน
  `GunnerGiantRocket:68` (จงใจเว้นให้ Stage 1)
- ห้ามแตะ `PlayerWeaponManager` `Physics.OverlapSphere` — เรียกต่อการยิง ไม่ใช่ต่อเฟรม คนละเรื่อง
- ห้ามเปลี่ยนค่าสมดุลเกม
- ห้ามรัน shell / build / unity-check — Claude รันเอง

## Manual Steps (ผู้ใช้ทำเองใน Unity Editor)

1. **ตาต้องเห็นเหมือนเดิมทุกอย่าง** — นี่คือเกณฑ์หลักของรอบนี้
   FloorHazard ต้องกระพริบแดงเหมือนเดิม · Tether ต้องสีเดิม · ศัตรูโดน freeze ต้องเป็นสีฟ้าน้ำแข็งเหมือนเดิม
2. **Profiler** — เปิด Profiler ตอนมีศัตรูเยอะๆ ดู **GC Alloc ต่อเฟรม** ควรลดลงชัดเจน
   (ก่อนแก้: `MineObject` + `playermove` + `Camera.main` จัดสรรทุกเฟรม)
3. **Memory** — เล่นสู้บอสยาวๆ แล้วดู Memory Profiler ว่า **Material count ไม่โตขึ้นเรื่อยๆ**
4. **โหลดฉากซ้ำ** — เข้าเกม → Quit to menu → เข้าใหม่ → กด F1 → **ต้องมีแผง DevTools แผงเดียว**

## ของที่เหลือหลังรอบนี้ — และทำไมไม่อยู่ในแผน

| ของ | ทำไมไม่ทำ |
|---|---|
| `MainBoss.GenerateLegacyConfig` รันทุก client | client อ่าน phase threshold จาก `config` — ยังวิเคราะห์ไม่ครบ |
| `FloorHazard.Activate()` ไม่มีใครเรียกใน C# | อาจเป็นฟีเจอร์ที่ยังไม่เสร็จ ไม่ใช่บั๊ก — ต้องถามเจ้าของงาน |
| `FenceWeapon.Update()` คัดลอก cooldown loop | เป็น override ที่ตั้งใจ (เพิ่ม `UpdateVisualBeams`) ไม่ใช่บั๊ก |
| `weaponVfxType: -1` บน 2 Fusion prefab · entry ตายใน `DefaultNetworkPrefabs` | ต้องแก้ `.prefab`/`.asset` — กฎห้าม เป็นงาน Inspector |
| `TelegraphZone:295` · `CharacterAnimationEvents:50` · `MineObject:79` · `ObjectiveOrb:147` ยัง `Instantiate` VFX | ต้องเพิ่ม entry ใน `VFXDatabase` ก่อน = งาน Editor |
| Stage 0.1 buff ไม่ทำงานสำหรับ client | ก้อนใหญ่พอเป็น Round ของตัวเอง · เป็นบั๊กที่ผู้เล่นเจอจริง |
