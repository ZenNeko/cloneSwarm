# Implementation Plan
> Generated by Claude (Architect) — 2026-07-29
> Goal: **N5** `Time.timeScale` มีเจ้าของเดียว (แก้ softlock จากการกด ESC) + **N6** FlowField bake บนทุก client
> Round: 2

## Context

ตอนนี้มี **4 ไฟล์เขียน `Time.timeScale` อิสระกัน ไม่รู้จักกัน** (เอกสาร audit นับไว้ 3 — ตกไปหนึ่ง):

| ไฟล์ | บรรทัด | เขียนอะไร |
|---|---|---|
| `SharedExperienceManager.cs` | `:225` `:234` `:291` | 0 ตอน upgrade/orb phase, 1 ตอนจบ (ยิงผ่าน ClientRpc ทุก client) |
| `UI/PauseMenuUI.cs` | `:85` `:102-103` `:112` `:119` | 0 ตอน pause, คืน `prevTimeScale` **3 จุด** |
| `WinLoseUI.cs` | `:121` `:128` | 0 ตอนจบเกม, 1 ตอนกลับเมนู |
| `DevTools.cs` | `:124` | slow-mo |

### บั๊กที่เกิดจริง

- **กด ESC ตอนเลือกการ์ด** → `Pause()` จำ `prevTimeScale = 0` → phase จบเซ็ตเป็น 1 →
  `Resume()` เขียน **0** กลับ → **เกมค้างถาวร ไม่มี UI ต้องโหลดฉากใหม่**
- **กด ESC หลังจบเกม** → `Resume()` เขียน 1 → **โลกเดินต่อหลังจอ win/lose**

### ทำไมถึงไม่ใช้ counter หรือ enum ตัวเดียวตรงๆ

การเรียกในโปรเจกต์นี้ **ไม่สมดุล** — counter จะติดลบหรือรั่ว:

- `EndUpgradePhaseClientRpc` ถูกเรียกจาก **2 เส้นทาง** (`:189` จบ upgrade · `:313` จบ orb phase
  ใช้ RPC ตัวเดียวกัน reuse) → resume ยิงซ้ำได้
- `WinLoseUI:121` ตั้ง 0 แล้ว**ไม่มี pop** เลยจนกว่าจะออกเมนู
- `PauseMenuUI` คืนค่า **3 จุด** (`Resume` · `OnDisable` · `OnQuitClicked`)

และ enum ตัวเดียวถือ 2 สถานะพร้อมกันไม่ได้ ซึ่งจำเป็นเพราะ**ตัดสินใจแล้วว่าให้กด ESC ซ้อน card UI ได้**

→ ทางที่รับได้ทั้งคู่: **flag set + enum ที่ derive จาก priority**
เป็น state machine เจ้าของเดียวที่ query ได้ (`GamePause.Current`) แต่ซ้อนได้และ idempotent

## Project Rules (ห้ามละเมิด)

- **หลังงานนี้ ห้ามมีไฟล์ไหนนอก `GamePause.cs` เขียน `Time.timeScale` อีก** — นี่คือ acceptance หลัก
- `GamePause` เป็น **static ธรรมดา ไม่ใช่ MonoBehaviour ไม่ใช่ NetworkBehaviour** —
  pause เป็นเรื่อง local ล้วน (`PauseMenuUI:23` ระบุว่าหยุด local-only คนอื่นเล่นต่อ)
- ห้ามแตะ `Library/`, `Temp/`, `obj/`, `Build/`
- ห้ามแตะ `.prefab` / `.unity` / `.asset` — งานนี้แก้เฉพาะ `.cs`
- ห้ามเปลี่ยน flow ของ upgrade/orb phase หรือเงื่อนไข win/lose — งานนี้ย้ายเจ้าของ `timeScale` อย่างเดียว

## Tasks

- [ ] T1: สร้าง `GamePause` — เจ้าของเดียวของ `Time.timeScale`
  - File: `Assets/Script/GamePause.cs` (**NEW**)
  - ```csharp
    using System.Collections.Generic;
    using UnityEngine;
    using UnityEngine.SceneManagement;

    /// <summary>เหตุผลที่เกมหยุด — เรียงตาม priority จากต่ำไปสูง</summary>
    public enum PauseReason
    {
        PauseMenu   = 0,   // ผู้เล่นกด ESC
        PhaseSelect = 1,   // upgrade / orb phase กำลังเลือกการ์ด
        GameOver    = 2,   // win/lose panel ขึ้นแล้ว
    }

    /// <summary>สถานะเกมที่ derive จากเหตุผลที่ active อยู่ — ตัวที่ priority สูงสุดชนะ</summary>
    public enum GameState { Playing, PauseMenu, PhaseSelect, GameOver }

    /// <summary>
    /// เจ้าของเดียวของ Time.timeScale — **ห้ามมีไฟล์อื่นเขียน Time.timeScale อีก**
    ///
    /// ทำไมเป็น flag set ไม่ใช่ counter: การเรียกในโปรเจกต์นี้ไม่สมดุล
    /// (EndUpgradePhase ยิงจาก 2 เส้นทาง · WinLoseUI ไม่เคย pop · PauseMenuUI คืนค่า 3 จุด)
    /// Add/Remove เป็น idempotent — เรียกซ้ำหรือ Remove ตัวที่ไม่ได้ถืออยู่ก็ไม่พัง
    /// </summary>
    public static class GamePause
    {
        static readonly HashSet<PauseReason> _active = new HashSet<PauseReason>();
        static float _resumeScale = 1f;

        /// <summary>ความเร็วตอนไม่ได้หยุด — DevTools slow-mo เขียนตัวนี้ ไม่ใช่ Time.timeScale</summary>
        public static float ResumeScale
        {
            get => _resumeScale;
            set { _resumeScale = Mathf.Max(0.01f, value); Apply(); }
        }

        public static bool IsPaused => _active.Count > 0;

        /// <summary>สถานะปัจจุบัน = เหตุผลที่ priority สูงสุด</summary>
        public static GameState Current
        {
            get
            {
                if (_active.Contains(PauseReason.GameOver))    return GameState.GameOver;
                if (_active.Contains(PauseReason.PhaseSelect)) return GameState.PhaseSelect;
                if (_active.Contains(PauseReason.PauseMenu))   return GameState.PauseMenu;
                return GameState.Playing;
            }
        }

        public static bool Has(PauseReason reason) => _active.Contains(reason);

        public static void Add(PauseReason reason)    { _active.Add(reason);    Apply(); }
        public static void Remove(PauseReason reason) { _active.Remove(reason); Apply(); }

        /// <summary>เคลียร์ทุกเหตุผล — เรียกอัตโนมัติตอนโหลดฉากใหม่</summary>
        public static void ResetAll() { _active.Clear(); _resumeScale = 1f; Apply(); }

        static void Apply() => Time.timeScale = _active.Count > 0 ? 0f : _resumeScale;

        // อัตโนมัติ — กันคนลืมเรียก ResetAll ตอนเปลี่ยนฉาก (เหมือน ServerSetMaxHealth ของ Round 1)
        [RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.SubsystemRegistration)]
        static void Init()
        {
            _active.Clear();
            _resumeScale = 1f;
            Time.timeScale = 1f;
            SceneManager.sceneLoaded -= OnSceneLoaded;
            SceneManager.sceneLoaded += OnSceneLoaded;
        }

        static void OnSceneLoaded(Scene s, LoadSceneMode m) => ResetAll();
    }
    ```
  - Why `SubsystemRegistration`: static field ไม่รีเซ็ตเองถ้าปิด Domain Reload ใน Enter Play Mode Options
    → ไม่ทำแบบนี้ เข้า play mode รอบสองจะได้ `_active` ค้างจากรอบก่อน
  - Why hook `sceneLoaded` ในตัวเอง: กันเคส "quit to menu แล้วเริ่มเกมใหม่" ได้ `GameOver` ค้าง
    ถ้าให้คนอื่นเรียก `ResetAll()` เอง จะเป็นบั๊กแบบเดียวกับที่กำลังแก้อยู่นี่แหละ
  - Expected: คอมไพล์ผ่าน ยังไม่มีใครเรียก

- [ ] T2: `SharedExperienceManager` เลิกเขียน `timeScale` เอง
  - File: `Assets/Script/SharedExperienceManager.cs` (MODIFY)
  - `:225` (`BeginUpgradePhaseClientRpc`) `Time.timeScale = 0f;`
    → `GamePause.Add(PauseReason.PhaseSelect);`
  - `:291` (`BeginOrbPhaseClientRpc`) `Time.timeScale = 0f;`
    → `GamePause.Add(PauseReason.PhaseSelect);`
  - `:234` (`EndUpgradePhaseClientRpc`) `Time.timeScale = 1f;`
    → `GamePause.Remove(PauseReason.PhaseSelect);`
  - **ห้ามแตะบรรทัดอื่นในไฟล์นี้** — `:210` `WaitForSecondsRealtime` ถูกอยู่แล้ว (timer ต้องเดินตอน pause)
  - Expected: ไม่มี `Time.timeScale` เหลือในไฟล์นี้
  - Why: `:313` `CompleteOrbPhase` reuse `EndUpgradePhaseClientRpc` ตัวเดียวกัน → เหตุผลเดียวกัน
    ครอบทั้งสอง phase และ `Remove` ซ้ำไม่พัง

- [ ] T3: `PauseMenuUI` — ทิ้ง `prevTimeScale` ทั้งชุด (ต้นตอของบั๊ก)
  - File: `Assets/Script/UI/PauseMenuUI.cs` (MODIFY)
  - **ลบ field** `:54` `float prevTimeScale = 1f;` ทิ้ง (ห้ามเหลือไว้ — นี่คือตัวจำค่า 0 ผิด)
  - `:102-103` ใน `Pause()` เดิม:
    ```csharp
    prevTimeScale  = Time.timeScale;
    Time.timeScale = 0f;
    ```
    → `GamePause.Add(PauseReason.PauseMenu);`
  - `:112` ใน `Resume()` `Time.timeScale = prevTimeScale;`
    → `GamePause.Remove(PauseReason.PauseMenu);`
  - `:85` ใน `OnDisable()` เดิม `if (isPaused) Time.timeScale = prevTimeScale;`
    → `if (isPaused) GamePause.Remove(PauseReason.PauseMenu);`
  - `:119` ใน `OnQuitClicked()` `Time.timeScale = 1f;`
    → `GamePause.ResetAll();`  *(ออกเมนูคือทิ้งทุกเหตุผล ไม่ใช่แค่ของตัวเอง)*
  - **แก้คอมเมนต์หัวไฟล์** `:12` `กด ESC → toggle pause (Time.timeScale = 0 / 1)`
    → `กด ESC → toggle pause (ผ่าน GamePause — ห้ามเขียน Time.timeScale ตรง)`
  - Expected: ไม่มี `Time.timeScale` และไม่มี `prevTimeScale` เหลือในไฟล์นี้
  - Why: `Pause()` จำค่า `Time.timeScale` ตอนนั้นแล้วเขียนกลับดื้อๆ คือสาเหตุของ softlock ทั้งสองเคส

- [ ] T4: `WinLoseUI` ใช้เหตุผล `GameOver`
  - File: `Assets/Script/WinLoseUI.cs` (MODIFY)
  - `:121` (ท้าย `ShowPanel`) `Time.timeScale = 0f;`
    → `GamePause.Add(PauseReason.GameOver);`
  - `:128` (`ReturnToMenu`) `Time.timeScale = 1f;`
    → `GamePause.ResetAll();`
  - **ห้ามแตะ** `:113` `Time.unscaledDeltaTime` — fade ต้องเดินตอน timeScale = 0
  - Expected: ไม่มี `Time.timeScale` เหลือในไฟล์นี้
  - Why: `GameOver` priority สูงสุด → กด ESC หลังจบเกมแล้ว Resume ถอดแค่ `PauseMenu`
    `GameOver` ยังค้าง โลกไม่เดินต่อ

- [ ] T5: `DevTools` slow-mo ผ่าน `ResumeScale`
  - File: `Assets/Script/DevTools.cs` (MODIFY)
  - `:124` `Time.timeScale = scale;` → `GamePause.ResumeScale = scale;`
  - Expected: ไม่มี `Time.timeScale` เหลือในไฟล์นี้
  - Why: slow-mo ที่ตั้งไว้ต้องรอดข้าม pause/resume — ถ้าเขียน `timeScale` ตรง
    pause ครั้งเดียวแล้ว resume จะเด้งกลับ 1 ทิ้ง slow-mo ไป

- [ ] T6: ตรวจว่าไม่มีใครหลงเหลือ
  - **ไม่แก้ไฟล์** — grep `Time.timeScale` ทั้ง `Assets/Script/`
    ต้องเจอเฉพาะใน `GamePause.cs` (ในเมธอด `Apply` และ `Init`) เท่านั้น
  - เจอที่อื่นให้เขียนไว้ใต้ `## Questions` **ห้ามแก้เอง**

---

- [ ] T7: FlowField เลิก bake บนทุก client (N6) — **คนละเรื่องกับ T1-T6 ไม่แตะไฟล์เดียวกัน**
  - File: `Assets/Script/FlowFieldPathfinder.cs` (MODIFY)
  - `Start()` `:103-113` **ลบบรรทัด `BakeWalkable();` ที่ `:112` ออกอย่างเดียว**
    ส่วน `_precalcDist` ด้านบน **เก็บไว้ทั้งหมด** (เป็นตาราง lookup เล็กๆ ไม่เกี่ยวกับ network)
  - `Update()` `:115-130` เดิม:
    ```csharp
    void Update()
    {
        if (!_baked) return;

        var nm = Unity.Netcode.NetworkManager.Singleton;
        if (nm != null && nm.IsListening && !nm.IsServer) return;

        if (Time.time < _nextUpdateAt) return;
    ```
    เปลี่ยนลำดับเป็น (**gate ขึ้นก่อน แล้วค่อย bake**):
    ```csharp
    void Update()
    {
        // gate ขึ้นก่อน — client ไม่ต้อง bake และไม่ต้อง compute
        var nm = Unity.Netcode.NetworkManager.Singleton;
        if (nm != null && nm.IsListening && !nm.IsServer) return;

        // bake ครั้งแรกที่ผ่าน gate — ไม่ทำใน Start() เพราะตอนนั้นยังไม่รู้ว่าเป็น server หรือ client
        if (!_baked) BakeWalkable();

        if (Time.time < _nextUpdateAt) return;
    ```
  - Expected: `BakeWalkable()` ถูกเรียกจาก `Update()` และ `RebakeWalkable()` เท่านั้น — ไม่มีใน `Start()` อีก
  - Why: `Start()` เรียก `BakeWalkable()` โดยไม่มี gate → `gridSize` 100×100 = **`Physics.CheckSphere` 10,000 ครั้ง**
    บนทุก client ตอนโหลดฉาก ทั้งที่ `Update()` มี gate อยู่แล้วเพราะ client ไม่ได้ใช้ flow field
    (`Enemy.cs:215,220` เป็นผู้ใช้รายเดียวและรันฝั่ง server)

  > ⚠️ **กับดัก — อ่านก่อนแก้** เอกสาร audit เขียนว่า "แก้บรรทัดเดียว" ซึ่ง**ผิดและอันตราย**
  > `BakeWalkable()` ไม่ได้แค่ยิง `CheckSphere` แต่ **allocate array ทั้ง 4 ตัว**
  > (`_walkable` `_costs` `_flow` `_densityMap` ที่ `:192-195`)
  >
  > **ห้ามใส่ `if (!IsServer) return;` ไว้ใน `Start()`** — ถ้า `NetworkManager` ยังไม่ `IsListening`
  > จังหวะที่ `Start()` รัน host จะไม่ bake **ตลอดทั้งเกม** → array เป็น null → enemy ยืนนิ่งทั้งแมพ
  > การ bake แบบ lazy ใน `Update()` แก้ปัญหานี้เอง เพราะกว่าจะถึงเฟรมแรกที่ผ่าน gate สถานะ network นิ่งแล้ว
  >
  > ปลอดภัยเพราะผู้อ่านทุกตัวเช็ค `_baked` อยู่แล้ว — `GetFlow` (`:139`) · `GetPathWaypoints` (`:159`)
  > คืน zero/list ว่างถ้ายังไม่ bake · offline (`nm == null`) ยัง bake ปกติเพราะ gate ปล่อยผ่าน

## Acceptance Criteria

- คอมไพล์ผ่าน 0 error
- `grep -rn "Time.timeScale" Assets/Script/` เจอเฉพาะใน `GamePause.cs`
- ไม่มี `prevTimeScale` เหลือในโปรเจกต์
- `PauseMenuUI` / `WinLoseUI` / `SharedExperienceManager` / `DevTools` เรียกผ่าน `GamePause` ทั้งหมด
- `FlowFieldPathfinder.Start()` ไม่มี `BakeWalkable()` แล้ว และ `Update()` gate ขึ้นก่อน bake

## Do NOT

- แก้ได้แค่ 6 ไฟล์นี้: `GamePause.cs` (ใหม่) · `SharedExperienceManager.cs` ·
  `UI/PauseMenuUI.cs` · `WinLoseUI.cs` · `DevTools.cs` · `FlowFieldPathfinder.cs`
- **T7 ห้ามแตะ `RebakeWalkable()`** (`:150`) — เป็น public API สำหรับตอน environment เปลี่ยน
  ตอนนี้ไม่มีใครเรียก แต่ไม่ใช่ dead code ที่ควรลบในงานนี้
- **T7 ห้ามลด `gridSize` หรือ `cellSize`** เพื่อให้ bake เร็วขึ้น — นั่นคือเปลี่ยนคุณภาพ pathfinding
  งานนี้คือ **ไม่ bake บนเครื่องที่ไม่ได้ใช้** ไม่ใช่ทำให้ bake ถูกลง
- **ห้ามเปลี่ยน `WaitForSecondsRealtime` / `Time.unscaledDeltaTime` เป็นตัวที่สเกลตาม timeScale**
  (`SharedExperienceManager:210` · `WinLoseUI:92,113`) — พวกนี้ต้องเดินตอนเกมหยุด ถูกอยู่แล้ว
- ห้ามทำ `GamePause` เป็น `MonoBehaviour` / singleton ที่ต้องวางใน scene — pause เป็น local ล้วน
  ถ้าต้องหา instance ใน scene จะพังตอน scene ยังโหลดไม่เสร็จ
- ห้าม sync `GamePause` ข้าม network — คนอื่นต้องเล่นต่อได้ตอนเรากด ESC (`PauseMenuUI:23`)
- ห้ามแตะ flow ของ upgrade/orb phase หรือเงื่อนไข win/lose
- ห้ามรัน shell / build / unity-check — Claude รันเอง

## Manual Steps (ผู้ใช้ทำเองใน Unity Editor)

1. **เคสหลักที่ต้องหาย** — เล่นจนขึ้น card UI → **กด ESC** → เมนู pause ขึ้นซ้อนบน card
   → กด Resume → **ต้องกลับไปเห็น card UI และเลือกได้ตามปกติ** เกมต้องไม่ค้าง
2. **เคสจบเกม** — ปล่อยให้แพ้/ชนะ → จอ win/lose ขึ้น → **กด ESC แล้ว Resume**
   → **โลกต้องไม่เดินต่อ** (enemy ต้องไม่ขยับหลังจอผลขึ้น)
3. **Orb Phase** — เก็บ orb ที่ทำให้ทุกคนได้การ์ด → เทสต์ ESC ซ้อนแบบข้อ 1 อีกครั้ง
4. **Quit แล้วเล่นใหม่** — จบเกม → Return to Menu → เริ่มเกมใหม่
   → **เกมต้องเดินปกติทันที** ไม่ค้างจาก `GameOver` ที่ค้างมาจากรอบก่อน
5. **DevTools slow-mo** — ตั้ง slow-mo แล้วกด ESC → Resume → **slow-mo ต้องยังอยู่** ไม่เด้งกลับปกติ
6. **2 client** — คนหนึ่งกด ESC → **อีกคนต้องเล่นต่อได้ปกติ** (pause เป็น local เท่านั้น)

### T7 (FlowField) — ต้องเทสต์แยก เพราะพลาดแล้วเกมพังเงียบ

7. **host ต้อง bake** — เข้าเกมเป็น host → Console ต้องขึ้น `[FlowField] Baked 100×100 grid — N blocked cells`
   **หนึ่งครั้ง** · แล้ว **enemy ต้องเดินเข้าหาผู้เล่นตามปกติ** ถ้ายืนนิ่งแปลว่า array ไม่ถูก allocate
8. **client ต้องไม่ bake** — จอ client **ต้องไม่มีบรรทัด `[FlowField] Baked` เลย** และ enemy
   ต้องยังขยับปกติบนจอ client (เห็นผ่าน NGO transform sync ไม่ได้ใช้ flow field)
9. **โหลดฉากซ้ำ** — Quit to menu → เริ่มเกมใหม่ → ต้องขึ้น `Baked` อีกครั้งและ enemy เดินได้
