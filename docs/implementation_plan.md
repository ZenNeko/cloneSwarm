# Implementation Plan
> Generated by Claude (Architect) — 2026-07-28
> Goal: แก้ P0 ทั้ง 4 ข้อจาก audit รอบสอง (N1 หลอดเลือดบอสผิดบน client · N2 orb เก็บซ้ำ ·
> N3 HitEffect ซ้อน · N4 ClusterBomb ระเบิดทั่วแมพ)
> Round: 1

## Context

ทั้ง 4 ข้อมาจาก audit ที่ Sonnet อ่านลึก 85 ไฟล์ และ **Claude verify ด้วยตัวเองแล้วทุกข้อ**
ทั้งหมดเป็นงานกล ไม่มีการตัดสินใจเชิงออกแบบ

### ⚠️ รูปแบบร่วมของทั้ง 4 ข้อ — "แก้ไฟล์เดียว ลืมพี่น้อง"

โปรเจกต์นี้มีไฟล์โครงเหมือนกันเป็นตระกูล และการแก้บั๊กที่ผ่านมาลงแค่ไฟล์เดียวทุกครั้ง

| แก้แล้วก่อนหน้า | พี่น้องที่ยังผิด (= งานรอบนี้) |
|---|---|
| `ExpOrb` มี `isCollected` | `HealingOrb` · `MagnetOrb` |
| `StormcallerWeapon` ส่ง `hitVfx: "None"` | `LightningChain` · `StormBunny` |
| `DeathFieldWeapon` มี distance gate | `ClusterBomb` |
| `playermove.GetHealthPercent` (commit `f95b7ef4`) | `Enemy.GetHealthPercent` · `WorldHPBar` |

**เพราะฉะนั้น T1 ให้เพิ่ม server-only setter ไม่ใช่แค่เซ็ตค่าตรงๆ ทีละจุด** — จะได้กันไม่ให้
คนเพิ่มจุดที่ 4 ในอนาคตแล้วลืมอีก

## Project Rules (ห้ามละเมิด)

- server-authoritative: `NetworkVariable` เขียนได้จาก server เท่านั้น
- VFX ใช้ `NetworkedVFXPool` · SFX ใช้ `SoundManager`
- **`HitEffect` / `CritHitEffect` ถูกยิงโดย `Enemy.NotifyHitClientRpc` อัตโนมัติอยู่แล้ว**
  weapon ห้ามยิงซ้ำ (`CLAUDE.md` ระบุว่าเคยเป็นบั๊กใน Laser/Raycast มาแล้ว)
- ห้ามแตะ `Library/`, `Temp/`, `obj/`, `Build/`, `scratch/`
- ห้ามแตะไฟล์ `.prefab` / `.unity` / `.asset` — งานนี้แก้เฉพาะ `.cs`

## Tasks

- [x] T1: เพิ่ม `netMaxHealth` ให้ `Enemy` + server-only setter (N1 ส่วนที่ 1)
  - File: `Assets/Script/Enemy.cs` (MODIFY)
  - **เพิ่ม NetworkVariable** ใต้ `netHealth` (บรรทัด 64-65):
    ```csharp
    /// <summary>maxHealth ที่ replicate แล้ว — client ต้องอ่านตัวนี้ ห้ามอ่าน field maxHealth
    /// เพราะ ApplyWaveScaling / Elite แก้ field เฉพาะฝั่ง server</summary>
    public NetworkVariable<float> netMaxHealth = new NetworkVariable<float>(
        30f, NetworkVariableReadPermission.Everyone, NetworkVariableWritePermission.Server);
    ```
  - **เพิ่ม setter server-only** (วางใกล้ `ApplyWaveScaling`):
    ```csharp
    /// <summary>ตั้ง maxHealth ฝั่ง server พร้อม replicate — ใช้ตัวนี้เสมอ ห้ามเขียน maxHealth ตรงๆ
    /// จากภายนอก ไม่งั้น client จะได้ค่าเก่า</summary>
    public void ServerSetMaxHealth(float value)
    {
        if (!IsServer) return;
        maxHealth          = value;
        netMaxHealth.Value = value;
    }
    ```
  - **แก้ 2 จุดที่เซ็ตอยู่แล้ว**
    - บรรทัด 98 (`OnNetworkSpawn`) เดิม `netHealth.Value = maxHealth;`
      → เพิ่มบรรทัดก่อนหน้า: `netMaxHealth.Value = maxHealth;`
      (ที่นี่เซ็ตตรงได้ เพราะอยู่ใน `Enemy` เองและอยู่หลัง `if (!IsServer) return;` แล้ว — **ตรวจให้แน่ใจว่าอยู่หลังจริง**)
    - `ApplyWaveScaling` บรรทัด 551-553 เดิม:
      ```csharp
      if (!IsServer) return;
      maxHealth       = maxHealth * healthMult;
      netHealth.Value = maxHealth;
      ```
      เปลี่ยนเป็น:
      ```csharp
      if (!IsServer) return;
      ServerSetMaxHealth(maxHealth * healthMult);
      netHealth.Value = maxHealth;
      ```
  - **แก้ `GetHealthPercent`** บรรทัด 574 เดิม `=> netHealth.Value / maxHealth;`
    → `=> netMaxHealth.Value > 0f ? netHealth.Value / netMaxHealth.Value : 0f;`
  - Expected: ไม่มีที่ไหนใน `Enemy.cs` หาร `netHealth.Value` ด้วย field `maxHealth` อีก

- [x] T2: ให้ Elite ใช้ setter (N1 ส่วนที่ 2)
  - File: `Assets/Script/Elite/EliteController.cs` (MODIFY)
  - บรรทัด 95-96 เดิม:
    ```csharp
    _enemy.maxHealth += def.shieldHP;
    _enemy.netHealth.Value = _enemy.maxHealth;
    ```
    เปลี่ยนเป็น:
    ```csharp
    _enemy.ServerSetMaxHealth(_enemy.maxHealth + def.shieldHP);
    _enemy.netHealth.Value = _enemy.maxHealth;
    ```
  - Expected: ไม่มีการเขียน `_enemy.maxHealth` ตรงๆ จากไฟล์นี้อีก

- [x] T3: หลอดเลือดบอสอ่านค่าที่ replicate (N1 ส่วนที่ 3)
  - File: `Assets/Script/UI/WorldHPBar.cs` (MODIFY)
  - บรรทัด 122-123 เดิม:
    ```csharp
    float pct = _enemy.maxHealth > 0f
        ? Mathf.Clamp01(_enemy.netHealth.Value / _enemy.maxHealth)
    ```
    เปลี่ยน `_enemy.maxHealth` ทั้งสองที่เป็น `_enemy.netMaxHealth.Value`
  - บรรทัด 136 เดิม:
    ```csharp
    $"{Mathf.CeilToInt(_enemy.netHealth.Value)} / {Mathf.CeilToInt(_enemy.maxHealth)}";
    ```
    เปลี่ยน `_enemy.maxHealth` → `_enemy.netMaxHealth.Value`
  - Expected: ไม่มี `_enemy.maxHealth` เหลือในไฟล์นี้
  - Why: mini-boss สเกล ×5 จาก 30 → client เห็น `Clamp01(150/30)` = หลอดเต็มค้าง และตัวเลข `"150 / 30"`

- [x] T4: กัน orb เก็บซ้ำ (N2)
  - File: `Assets/Script/HealingOrb.cs` และ `Assets/Script/MagnetOrb.cs` (MODIFY ทั้งสอง)
  - **ลอกรูปแบบจาก `ExpOrb.cs:89-95` เป๊ะๆ** — เพิ่ม field ก่อนเมธอด `Collect()`:
    ```csharp
    private bool isCollected = false;
    ```
    แล้วใส่ 2 บรรทัดแรกใน `Collect()`:
    ```csharp
    if (isCollected) return;
    isCollected = true;
    ```
  - `HealingOrb` มีสองทางเข้า `Collect()` ที่บรรทัด 73 (`Update` เช็คระยะ) และ 111 (`OnTriggerEnter`)
    `MagnetOrb` เช่นกันที่บรรทัด 60 และ 113
  - Expected: ทั้งสองไฟล์มีโครงเหมือน `ExpOrb` ทุกประการ
  - Why: `Despawn`/`Destroy` มีผลสิ้นเฟรม → เข้าสองทางในเฟรมเดียวได้ → `HealPercent()` ทำงาน 2 ครั้ง

- [x] T5: ลบ HitEffect ที่ยิงซ้ำ (N3)
  - File: `Assets/Script/Weapon/ValorWeapon.cs` (MODIFY)
    บรรทัด 138-139 **ลบทิ้งทั้งสองบรรทัด**:
    ```csharp
    string baseHit = isCrit ? "CritHitEffect" : "HitEffect";
    manager.BroadcastVfxTypeServerRpc(transform.position, baseHit);
    ```
    (บรรทัด 137 `FireMelee(...)` **เก็บไว้** — มันทำให้ `Enemy` ยิง HitEffect ให้อยู่แล้ว)
    ถ้าตัวแปร `baseHit` ไม่ถูกใช้ที่อื่นแล้วให้ลบไปด้วย **แต่ถ้าถูกใช้ต่อ อย่าลบ ให้เขียนไว้ใต้ `## Questions`**
  - File: `Assets/Script/Weapon/LightningChainWeapon.cs` (MODIFY)
    บรรทัด 28 `hitVfx: "HitEffect",` → `hitVfx: "None",`
  - File: `Assets/Script/Weapon/StormBunnyWeapon.cs` (MODIFY)
    บรรทัด 114 และ 145 `hitVfx: "HitEffect",` → `hitVfx: "None",` **ทั้งสองจุด**
  - Expected: ไม่มีไฟล์ไหนใน `Weapon/` ส่ง `"HitEffect"` เข้า `FireChainServerRpc` อีก
  - Why: `PlayerWeaponManager.cs:1344-1346` เรียก `EnemyTakeDamage` (HitEffect อัตโนมัติ) แล้ว
    `BroadcastBeamClientRpc` → `VFXFactory.cs:48` ยิงซ้ำทุกข้อของ chain ·
    `StormcallerWeapon.cs:46` ที่ใช้ RPC ตัวเดียวกันส่ง `"None"` อยู่แล้ว = แบบที่ถูก

- [x] T6: ClusterBomb ระเบิดเฉพาะศัตรูที่ตายใกล้ตัว (N4)
  - File: `Assets/Script/Weapon/ClusterBombWeapon.cs` (MODIFY)
  - เมธอดเดิม บรรทัด 95-101:
    ```csharp
    void OnEnemyKilled(Vector3 deathPos)
    {
        if (manager == null || !manager.IsOwner) return;

        // Defer — กัน recursive call ทำ stack overflow ตอน chain kill หลายตัว
        _pendingKillExplosions.Add(deathPos);
    }
    ```
  - เปลี่ยนเป็น (**ลอกรูปแบบจาก `DeathFieldWeapon.cs:60-68`**):
    ```csharp
    void OnEnemyKilled(Vector3 deathPos)
    {
        if (manager == null || !manager.IsOwner) return;

        var ld = data != null ? data.GetLevelData(currentLevel) : null;
        if (ld == null) return;

        // ระเบิดเฉพาะ enemy ที่ตายในระยะของอาวุธเรา — ไม่ใช่ทั้งแมพ
        float triggerRadius = ld.range * (manager.statManager != null
            ? manager.statManager.GetAreaMultiplier() : 1f);
        if (Vector3.Distance(transform.position, deathPos) > triggerRadius) return;

        // Defer — กัน recursive call ทำ stack overflow ตอน chain kill หลายตัว
        _pendingKillExplosions.Add(deathPos);
    }
    ```
  - Expected: โครงตรงกับ `DeathFieldWeapon.OnEnemyDiedAt` ทุกประการ ต่างแค่ชื่อ list ปลายทาง
  - Why: `Enemy.OnAnyEnemyDiedAt` เป็น static event ระดับโลก — ไม่มี gate = ศัตรูตายที่ไหนก็ได้
    รวมถึงที่เพื่อนฆ่า ก็ยิงระเบิด + ลูกย่อยให้ฟรีทั้งเกม

## Acceptance Criteria

- คอมไพล์ผ่าน 0 error
- ไม่มีที่ไหนหาร `netHealth.Value` ด้วย field `maxHealth` อีก (ทั้ง `Enemy.cs` และ `WorldHPBar.cs`)
- `HealingOrb` / `MagnetOrb` มี `isCollected` guard เหมือน `ExpOrb`
- ไม่มี weapon ไหนยิง `HitEffect` / `CritHitEffect` เอง
- `ClusterBombWeapon.OnEnemyKilled` มีการเช็คระยะ

## Do NOT

- แก้ได้แค่ 9 ไฟล์นี้: `Enemy.cs` · `Elite/EliteController.cs` · `UI/WorldHPBar.cs` ·
  `HealingOrb.cs` · `MagnetOrb.cs` · `Weapon/ValorWeapon.cs` · `Weapon/LightningChainWeapon.cs` ·
  `Weapon/StormBunnyWeapon.cs` · `Weapon/ClusterBombWeapon.cs`
- ห้ามแตะ `ExpOrb.cs`, `DeathFieldWeapon.cs`, `StormcallerWeapon.cs` — เป็น**ต้นแบบที่ถูกอยู่แล้ว** ใช้ลอกเท่านั้น
- ห้ามเปลี่ยน `maxHealth` เป็น private หรือลบทิ้ง — มีที่อื่นอ่านอยู่ งานนี้แค่เพิ่มตัว replicate
- ห้ามเปลี่ยนสมดุลเกม (ค่า damage / radius / cooldown) — T6 คือการ**คืน**พฤติกรรมที่ตั้งใจ ไม่ใช่ปรับบาลานซ์
- ห้ามรันคำสั่ง shell / build / unity-check — Claude เป็นคนรันเอง

## Manual Steps (ผู้ใช้ทำเองใน Unity Editor)

1. **T1 เพิ่ม NetworkVariable ใหม่** — `netMaxHealth` จะโผล่เป็นค่า default 30 ตอน runtime
   ตรวจว่า enemy prefab ทุกตัวยังตั้ง `maxHealth` ใน Inspector ถูกต้อง (โค้ดอ่านค่านั้นไปใส่ตอน spawn)
2. **เทสต์ N1 แบบ 2 client** — host + client สู้ mini-boss (ตัวที่มี wave scaling)
   → **หลอดเลือดบนจอ client ต้องลดลงเรื่อยๆ ตามจริง** ไม่ใช่เต็มค้างแล้วฮวบท้าย
   → ตัวเลขต้องเป็น `เลือดปัจจุบัน / เลือดสูงสุดที่สเกลแล้ว` เช่น `"90 / 150"` ไม่ใช่ `"150 / 30"`
   ทดสอบกับ Elite ที่มี Shield ด้วย
3. **เทสต์ N2** — เดินเก็บ Healing Orb เร็วๆ ให้ชนพอดีตอนอยู่ในระยะดูดด้วย → ต้องฮีลรอบเดียว
4. **เทสต์ N3** — ใช้ Valor / Lightning Chain / Storm Bunny → เอฟเฟกต์ตอนโดนต้องขึ้น**ชั้นเดียว**
   ไม่ซ้อนกันสองชั้น และ Valor ที่ dash ไม่โดนใครต้องไม่มีเอฟเฟกต์เด้งที่ตัวเอง
5. **เทสต์ N4** — ถือ Cluster Bomb แล้วให้เพื่อนฆ่าศัตรูไกลๆ → **ต้องไม่มีระเบิดเกิดขึ้น**
   ฆ่าเองใกล้ๆ → ระเบิดตามปกติ

## Changed Files
- [Assets/Script/Enemy.cs](file:///e:/Zenity%20Why%20not/cloneSwarm/Assets/Script/Enemy.cs): เพิ่ม `netMaxHealth` NetworkVariable, เพิ่ม `ServerSetMaxHealth()` setter ฝั่ง server, และปรับ `GetHealthPercent()` ให้อ่านจาก `netMaxHealth`
- [Assets/Script/Elite/EliteController.cs](file:///e:/Zenity%20Why%20not/cloneSwarm/Assets/Script/Elite/EliteController.cs): ปรับการเพิ่ม shield HP ให้เรียก `ServerSetMaxHealth()` แทนการเขียน `maxHealth` ตรงๆ
- [Assets/Script/UI/WorldHPBar.cs](file:///e:/Zenity%20Why%20not/cloneSwarm/Assets/Script/UI/WorldHPBar.cs): เปลี่ยนการคำนวณและแสดงผลหลอด HP ให้อ่าน `netMaxHealth.Value` แทน field `maxHealth`
- [Assets/Script/HealingOrb.cs](file:///e:/Zenity%20Why%20not/cloneSwarm/Assets/Script/HealingOrb.cs): เพิ่ม `isCollected` guard ป้องกันการเรียก `Collect()` ซ้อนในเฟรมเดียวกัน
- [Assets/Script/MagnetOrb.cs](file:///e:/Zenity%20Why%20not/cloneSwarm/Assets/Script/MagnetOrb.cs): เพิ่ม `isCollected` guard ป้องกันการเรียก `Collect()` ซ้อนในเฟรมเดียวกัน
- [Assets/Script/Weapon/ValorWeapon.cs](file:///e:/Zenity%20Why%20not/cloneSwarm/Assets/Script/Weapon/ValorWeapon.cs): ลบการเรียก `BroadcastVfxTypeServerRpc(..., baseHit)` ซ้ำซ้อน (เนื่องจาก `Enemy` แสดง VFX ผ่าน `NotifyHitClientRpc` อยู่แล้ว)
- [Assets/Script/Weapon/LightningChainWeapon.cs](file:///e:/Zenity%20Why%20not/cloneSwarm/Assets/Script/Weapon/LightningChainWeapon.cs): เปลี่ยน `hitVfx: "HitEffect"` เป็น `"None"` ป้องกัน VFX แสดงซ้ำซ้อน
- [Assets/Script/Weapon/StormBunnyWeapon.cs](file:///e:/Zenity%20Why%20not/cloneSwarm/Assets/Script/Weapon/StormBunnyWeapon.cs): เปลี่ยน `hitVfx: "HitEffect"` เป็น `"None"` ทั้ง 2 จุด ป้องกัน VFX แสดงซ้ำซ้อน
- [Assets/Script/Weapon/ClusterBombWeapon.cs](file:///e:/Zenity%20Why%20not/cloneSwarm/Assets/Script/Weapon/ClusterBombWeapon.cs): เพิ่ม distance gate เช็คระยะ `Vector3.Distance` ให้ระเบิดเฉพาะศัตรูที่ตายในระยะอาวุธเท่านั้น

### เพิ่มโดย Claude ตอน verify (ไฟล์ที่ 10 — **แผนตกไปเอง**)

- `Assets/Script/UI/MiniBossBarEntry.cs` — `:71-72` `:85` อ่าน `netMaxHealth.Value` แทน field `maxHealth`
  บั๊กเดียวกับ `WorldHPBar` เป๊ะ และเป็น **หลอด mini-boss ใน HUD list** ซึ่งคือเคสที่ N1 พูดถึงตรงๆ
  → mini-boss โผล่ 2 ที่: หลอดในโลก (`WorldHPBar`, T3 แก้แล้ว) + แถบใน HUD (ตัวนี้)
  **แผนรอบนี้เตือนเรื่อง "แก้ไฟล์เดียว ลืมพี่น้อง" แล้วยังพลาดเอง** — grep ก่อนปิดงานคือตัวที่จับได้

### ตรวจแล้วไม่ใช่บั๊ก — ไม่ต้องแก้

- `BossController.cs:84` · `EliteController.cs:113,121` อ่าน field `maxHealth` แต่อยู่หลัง
  `if (!IsServer) return;` ทั้งคู่ → เป็น logic ฝั่ง server ค่าถูกเสมอ

### ค้างไว้ตั้งใจ (dev tool — อย่าแก้แบบไม่คิด)

- `WeaponTestManager.cs:150` `enemy.maxHealth = maxHp;` เขียน field ตรง ข้าม `ServerSetMaxHealth`
  **ห้ามเปลี่ยนเป็น setter แบบมักง่าย** — ถ้าเครื่องมือนี้ตั้งค่าก่อน `NetworkObject.Spawn`
  `IsServer` จะเป็น false → setter no-op เงียบๆ = พังเครื่องมือเทสต์ ต้องเช็ค call site ก่อน
